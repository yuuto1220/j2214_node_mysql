<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
        integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">
  <style>
    body {
      background: linear-gradient(135deg, #f8fafc 0%, #e0e7ef 100%);
      min-height: 100vh;
    }
    .card {
      margin-top: 80px;
      box-shadow: 0 4px 24px rgba(0,0,0,0.08);
      border-radius: 1rem;
    }
    .logo {
      font-size: 2.2rem;
      font-weight: bold;
      color: #007bff;
      letter-spacing: 2px;
      margin-bottom: 1rem;
    }
    .welcome-title {
      font-size: 1.7rem;
      font-weight: 600;
      color: #333;
      margin-bottom: 1rem;
      letter-spacing: 1px;
    }
    .welcome-desc {
      color: #555;
      font-size: 1.1rem;
      margin-bottom: 2rem;
    }
    .btn-auth {
      width: 48%;
      font-size: 1.1rem;
      padding: 0.75rem 0;
    }
    @media (max-width: 576px) {
      .btn-auth {
        width: 100%;
        margin-bottom: 1rem;
      }
    }
  </style>
</head>
<body>
  <%- include('./header'); %>
  <% if(isAuth) { %>
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card p-4">
            <div class="logo text-center">TodoApp</div>
            <h4 class="text-center mb-4">タスクの追加</h4>
            <form action="/" method="post" id="taskForm">
              <div class="form-group">
                <label for="add">タスク内容</label>
                <input required type="text" name="add" id="add" placeholder="何をする?" class="form-control mb-2" />
              </div>
              <div class="form-group">
                <label for="dueDate">期日</label>
                <input type="date" name="dueDate" id="dueDate" class="form-control mb-2" required />
              </div>
              <input type="submit" value="追加" class="btn btn-primary btn-block" />
            </form>
            <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
              <div class="alert alert-danger mt-3"><%= errorMessage %></div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
    <script>
      // 期日が未入力の場合は送信できないようにする
      document.getElementById('taskForm').addEventListener('submit', function(e) {
        const dueDate = document.getElementById('dueDate').value;
        if (!dueDate) {
          e.preventDefault();
          alert('期日を入力してください。');
        }
      });
    </script>
  <% } else { %>
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card p-5 text-center">
            <div class="logo">TodoApp</div>
            <div class="welcome-title">Welcome to TodoApp</div>
            <div class="welcome-desc">
              TodoAppはあなたの毎日をサポートするタスク管理アプリです。<br>
              サインインまたはサインアップして始めましょう！
            </div>
            <div class="d-flex justify-content-between flex-wrap">
              <a class="btn btn-primary btn-auth mb-2" href="/signin" role="button">サインイン</a>
              <a class="btn btn-outline-primary btn-auth mb-2" href="/signup" role="button">サインアップ</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% } %>
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
          integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
          crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.6/umd/popper.min.js"
          integrity="sha384-wHAiFfRlMFy6i5SRaxvfOCifBUQy1xHdJ/yoi7FRNXMRBu5WHdZYu1hA6ZOblgut"
          crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/js/bootstrap.min.js"
          integrity="sha384-B0UglyR+jN6CkvvICOB2joaf5I4l3gm9GU6Hc1og6Ls7i6U/mkkaduKaBhlAXv9k"
          crossorigin="anonymous"></script>
</body>
</html>